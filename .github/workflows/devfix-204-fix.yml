name: DevFix 2.0.4 final validation fix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/devfix-204-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply 2.0.4 patch
        run: |
          set -euo pipefail
          python3 .github/bootstrap/devfix_204_patch.py
          git checkout HEAD -- .github/workflows/devfix-204-fix.yml
      - name: Validate
        run: |
          set -euo pipefail
          bash -n bin/devfix install.sh uninstall.sh scripts/*.sh tests/*.sh
          bash tests/test_devfix.sh
          bash tests/test_install.sh
          git diff --check
      - name: Commit to main
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Fix Snowflake route validation after bootstrap'
          git push origin HEAD:main
