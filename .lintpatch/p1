diff -ruN '--exclude=.git' '--exclude=.github' a/bin/devfix b/bin/devfix
--- a/bin/devfix	2026-08-15 16:25:30.000000000 +0000
+++ b/bin/devfix	2026-08-15 16:34:59.088108504 +0000
@@ -41,14 +41,12 @@
 DEFAULT_SNOWFLAKE_BRIDGE='snowflake 192.0.2.3:80 2B280B23E1107BB62ABFC40DDCC8824814F80A72 fingerprint=2B280B23E1107BB62ABFC40DDCC8824814F80A72 url=https://1098762253.rsc.cdn77.org front=www.phpmyadmin.net,cdn.zk.mk ice=stun:stun.antisip.com:3478,stun:stun.epygi.com:3478,stun:stun.uls.co.za:3478,stun:stun.voipgate.com:3478,stun:stun.mixvoip.com:3478,stun:stun.nextcloud.com:3478,stun:stun.bethesda.net:3478,stun:stun.nextcloud.com:443 utls-imitate=hellorandomizedalpn'
 
 C_RESET=''
-C_BOLD=''
 C_GREEN=''
 C_YELLOW=''
 C_RED=''
 C_BLUE=''
 if [ -t 1 ] && [ -z "${NO_COLOR:-}" ]; then
   C_RESET='\033[0m'
-  C_BOLD='\033[1m'
   C_GREEN='\033[32m'
   C_YELLOW='\033[33m'
   C_RED='\033[31m'
@@ -201,7 +199,7 @@
   cb=$(curl_bin) || { printf '000'; return 1; }
   case "$mode" in
     direct)
-      http_proxy= https_proxy= all_proxy= HTTP_PROXY= HTTPS_PROXY= ALL_PROXY= \
+      http_proxy='' https_proxy='' all_proxy='' HTTP_PROXY='' HTTPS_PROXY='' ALL_PROXY='' \
         "$cb" -L -sS -o /dev/null -w '%{http_code}' --noproxy '*' \
         --connect-timeout "$CONNECT_TIMEOUT" --max-time "$MAX_TIME" "$url" 2>/dev/null || printf '000'
       ;;
@@ -809,7 +807,7 @@
     printf 'Recommendation: use direct; DevFix will avoid unnecessary tunneling.\n'
   elif snowflake_available; then
     printf 'Primary issue: NETWORK_BLOCKED_OR_UNREACHABLE\n'
-    printf 'Recommendation: run `devfix connect`; auto mode will try built-in Snowflake.\n'
+    printf "%s\n" 'Recommendation: run `devfix connect`; auto mode will try built-in Snowflake.'
   else
     printf 'Primary issue: NETWORK_BLOCKED_OR_UNREACHABLE\n'
     printf 'Recommendation: reinstall the self-contained DevFix package; Snowflake payload is missing.\n'
diff -ruN '--exclude=.git' '--exclude=.github' a/install.sh b/install.sh
--- a/install.sh	2026-08-15 16:27:25.000000000 +0000
+++ b/install.sh	2026-08-15 16:34:59.087266065 +0000
@@ -1,6 +1,6 @@
 #!/bin/bash
 set -euo pipefail
-ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
+ROOT=$(cd -- "$(dirname -- "$0")" && pwd)
 PREFIX=${PREFIX:-/usr/local}
 DESTDIR=${DESTDIR:-}
 TARGET="$DESTDIR$PREFIX"
diff -ruN '--exclude=.git' '--exclude=.github' a/scripts/build-dist.sh b/scripts/build-dist.sh
--- a/scripts/build-dist.sh	2026-08-15 16:23:22.000000000 +0000
+++ b/scripts/build-dist.sh	2026-08-15 16:34:59.087503921 +0000
@@ -1,6 +1,6 @@
 #!/bin/bash
 set -euo pipefail
-ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
+ROOT=$(cd -- "$(dirname -- "$0")/.." && pwd)
 VERSION=$(cat "$ROOT/VERSION")
 DIST="$ROOT/dist"
 BUILD="$ROOT/build"
diff -ruN '--exclude=.git' '--exclude=.